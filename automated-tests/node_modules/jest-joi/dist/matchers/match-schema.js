"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toMatchSchema = void 0;
const classes_1 = require("../classes");
function toMatchSchema(receivedInput, schemaInput, optionsInput) {
    const name = "toMatchSchema";
    const received = new classes_1.Received(receivedInput);
    const options = new classes_1.Options(optionsInput);
    const schema = new classes_1.Schema(schemaInput);
    const result = new classes_1.Result(received, schema, options);
    const message = new classes_1.MatchMessage(this, name, result, received, schema);
    return {
        message: message.fn,
        pass: result.pass,
    };
}
exports.toMatchSchema = toMatchSchema;
